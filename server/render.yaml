# WineDash Render Deployment Configuration
# These settings configure how Render builds and runs the server

startCommand: node server.js
buildCommand: npm install && npx prisma generate && npx prisma migrate deploy
